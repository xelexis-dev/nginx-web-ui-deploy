# 업그레이드 경로

대상 버전: **1.17.1**

**1.17.1까지는 반드시 설치하고 넘어가야 하는 중간 버전이 없습니다.** 아래 준비 사항을 확인한 뒤 현재 버전에서 1.17.1로 바로 업그레이드할 수 있습니다. 필요한 데이터 변경은 시작할 때 순서대로 적용됩니다.

## 버전별 순서

| 현재 버전 | 설치 순서 | 필수 경유 버전 | 먼저 확인할 사항 |
| --- | --- | --- | --- |
| 1.17.1 | 유지 | 없음 | 이미 대상 버전입니다. |
| 1.17.0 | 1.17.0 → 1.17.1 | 없음 | 기존 1.17.x 게이트웨이·노드와 연결을 유지합니다. |
| 1.16.x | 1.16.x → 1.17.1 | 없음 | 연결된 게이트웨이와 노드를 모두 1.17.x로 맞춥니다. |
| 1.6.x ~ 1.15.x | 현재 버전 → 1.17.1 | 없음 | DB와 앱 데이터의 암호화 키를 함께 보존하고, Stream 포트 충돌을 확인합니다. |
| 1.3.x ~ 1.5.x | 현재 버전 → 1.17.1 | 없음 | 앱 데이터의 영구 보관과 백업을 확인합니다. 업데이트 후 암호화된 값을 구버전에서 읽지 못할 수 있습니다. |
| 1.2.x | 1.2.x → 1.17.1 | 없음 | DB·앱 데이터 연결을 유지하고 로그 볼륨 두 개를 추가합니다. |
| 버전 미표시·초기 latest·현재 버전 불명 | 현재 설치 확인 → 경로 결정 | 판단 전 | 사용 중인 이미지 버전과 DB·앱 데이터 위치를 먼저 확인합니다. |

예를 들어 **1.12.0 → 1.17.1**, **1.16.0 → 1.17.1**로 진행할 수 있습니다. 중간에 1.14.3이나 1.17.0을 별도로 설치할 필요는 없습니다.

이 표는 기본 이미지와 정상적으로 보존된 데이터를 사용하는 설치를 기준으로 합니다. 별도로 수정한 이미지나 자체 데이터 경로를 사용하는 설치는 백업 복사본으로 새 이미지가 기존 데이터를 읽는지 먼저 확인하세요.

## 건너뛰어도 적용해야 하는 준비 사항

### DB와 앱 데이터는 함께 보존

다음 두 경로에 연결된 **기존 볼륨 또는 호스트 폴더를 그대로 유지**하고 함께 백업합니다.

- `/var/lib/postgresql/data`: 사용자와 프록시 설정 등 데이터베이스
- `/var/lib/nginx-web-ui`: 인증서, 로그인·암호화 키, DB 접속 정보

업데이트 과정에서 새 빈 볼륨을 연결하면 기존 설정이 없는 설치로 시작됩니다. 앱 데이터만 잃어도 저장된 비밀번호나 인증서를 복구하지 못할 수 있습니다. 데이터 폴더 자체를 복사해 백업할 때는 서비스를 정상 종료한 뒤 복사하세요.

초기 설치에서 앱 데이터를 `/var/lib/nginx-control`에 연결했다면, **같은 기존 데이터를** 새 컨테이너의 `/var/lib/nginx-web-ui`에 연결해야 합니다.

1.2.x에서 올라오는 경우에는 `/var/log/nginx`와 `/var/log/nginx-web-ui`도 영구 보관하도록 연결합니다. [설치 안내의 볼륨 구성](README.md#영구-보관되는-데이터)을 참고하세요.

### 여러 노드를 관리한다면 버전을 함께 맞추기

**1.16.x와 1.17.x 사이에서는 게이트웨이·노드 관리 연결이 차단됩니다.** 관리하는 쪽과 관리받는 쪽을 모두 1.17.x로 업데이트해야 합니다.

백업과 이미지를 먼저 준비하고, 연결된 노드와 게이트웨이를 같은 점검 시간에 교체하세요. 교체 순서에 관계없이 버전이 다른 동안 관리 연결이 잠시 끊길 수 있습니다. 모든 교체가 끝나면 다시 연결되는지 확인합니다. 1.17.0과 1.17.1은 서로 연결할 수 있습니다.

### 기존 프록시와 Stream 확인

- 프록시 대상으로 지정한 컨테이너가 실행 중이며 같은 Docker 네트워크에 연결돼 있는지 확인합니다. 대상 이름을 찾을 수 없으면 새 컨테이너에서 설정 적용이 실패하고 프록시가 404를 반환할 수 있습니다.
- 1.15.x 이하에서 올라오며 Stream을 사용한다면, 관리 화면·웹 앱·DB·기존 리버스 프록시와 TCP 포트가 겹치는 항목을 먼저 수정합니다. 충돌하는 Stream은 새 버전에서 시작 시 비활성화될 수 있습니다.
- 관리 화면을 특정 호스트 주소에 바인딩했다면 그 주소가 현재도 유효한지 확인합니다. 포트를 이미 사용하는 다른 서비스가 있는지도 확인하세요.

## 업그레이드 실행

1. 위 표와 준비 사항을 확인하고 DB·앱 데이터, 기존 실행 설정, 구 이미지의 복구 수단을 확보합니다.
2. 기존 설치 폴더에서 Compose의 이미지 태그를 아래처럼 바꿉니다. 포트·볼륨·네트워크·환경변수는 기존 설치에 맞게 유지합니다.

   | Docker 아키텍처 | 이미지 |
   | --- | --- |
   | AMD64 | `nexus.xelexis.com/nginx-web-ui:1.17.1` |
   | ARM64 | `nexus.xelexis.com/nginx-web-ui:1.17.1-arm64` |

3. 기존 Compose 파일에 대해 설정 확인·다운로드·교체를 실행합니다. 파일명이 다르면 아래 `compose.yaml`을 기존 이름으로 바꿉니다.

   ```sh
   docker compose -f compose.yaml config --quiet
   docker compose -f compose.yaml pull
   docker compose -f compose.yaml up -d
   ```

AMD64에서 ARM64로 바꾸는 경우에는 이미지뿐 아니라 기존 `platform: linux/amd64` 고정도 확인합니다. 기존 설치에 별도 수정이 있다면 그대로 보존 가능한지 확인하고, DB·앱 데이터를 함께 백업한 뒤 진행하세요.

## 교체 후 확인과 복구

- 기존 관리자 계정으로 로그인되고, 사용자·프록시·인증서가 그대로인지 확인합니다. 기존 설치인데 최초 관리자 생성 화면이 나오면 새 관리자를 만들기 전에 데이터 연결부터 확인하세요.
- nginx·DB 검사와 실제 프록시 응답, HTTPS, 게이트웨이·노드 연결을 확인합니다. [설치 성공 확인](README.md#4-설치-성공-확인)의 명령을 사용할 수 있습니다.
- 관리 화면이 열린다는 이유만으로 완료로 판단하지 마세요. 부팅 중 설정 적용 실패 알림이 있으면 프록시가 동작하지 않을 수 있습니다.
- 되돌릴 때는 구 이미지와 기존 실행 설정을 사용합니다. 데이터가 변경되거나 암호화된 경우에는 이미지만 되돌려도 복구되지 않을 수 있으므로, 업데이트 전의 DB·앱 데이터 백업을 함께 복원해야 합니다.

이 경로는 **1.17.1까지**의 안내입니다. 이후 버전으로 올릴 때는 그 버전을 대상으로 갱신된 경로를 먼저 확인하세요.
